'use strict';

/**
 * Module dependencies.
 */
var should = require('should');
var processor = require('../controllers/subscriptions.server.controller');

/**
 * Unit tests
 */
describe('Subscription Controllers Unit Tests:', function() {
    beforeEach(function(done) {
        done();
    });

    describe('Nothing here yet', function() {
        it('This test does nothing.', function(done) {
            done();
        });
    });


    afterEach(function(done) {
        done();
    });
});